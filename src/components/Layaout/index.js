import React from "react";

const Layaout = ({children}) => {
    return (
        <section>
            <header>
                Hola
            </header>
            {children}
        </section>
    );
}

export default Layaout